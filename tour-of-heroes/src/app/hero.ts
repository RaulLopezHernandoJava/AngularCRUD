//La Interface no se declara y se implementan los metodos como en Java sino mas bien se declara para obligar al objeto
// Hero a tener los siguientes atributos . A que cumpla estructura de objeto

export interface Hero{
  id: number;
  name: string;
}
